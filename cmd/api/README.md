# Pi-Apps Go `cmd/api` package

This directory contains the command-line interface for the `api` package for other apps to talk to which ultimately gets compiled into a `api-go` binary that the `api` wrapper uses when install scripts call Pi-Apps Go functions.
